import os # استيراد مكتبة التعامل مع نظام التشغيل
from aiogram import Bot, Dispatcher
# ... باقي الاستيرادات

# قراءة التوكن من متغيرات البيئة (Environment Variables)
API_TOKEN = os.getenv('BOT_TOKEN')

if not API_TOKEN:
raise ValueError("لم يتم العثور على التوكن! تأكد من إضافته في إعدادات Render")

# ... باقي الكود كما هو